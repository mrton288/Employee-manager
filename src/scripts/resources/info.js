const MISAInfo = {
    TextGender: {
        Female: "Nữ",
        Male: "Nam",
        Other: "Không xác định",
    },
    RegexpFormat: {
        Phone: /^\(?(\d{3})\)?[- ]?(\d{3})[- ]?(\d{4})$/,
        Email: /^[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+@[a-zA-Z0-9-]+(?:\.[a-zA-Z0-9-]+)*$/,
    },
};

export default MISAInfo;
